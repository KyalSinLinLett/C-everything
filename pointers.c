#include <stdio.h>

int main(void)
{

    // int* p, c;
    // c = 5;
    // p = &c; // integer pointer p points to the address of c, stores the memory address of c
    // printf("%p\n", p); // p contains a memory address
    // printf("%d\n", *p); // *p returns the value inside the address that it is pointing to
    // printf("%p\n", &p); // &p shows that the pointer itself has a place in memory
    // printf("%d\n", *p == c);

    // int c = 5;
    // int* p = &c;
    // printf("%d", *p);

    // int x[4];
    // int i;

    // for (i = 0; i < 4; i++)
    // {
    //     printf("&x[%d] = %p\n", i, &x[i]);
    // }

    // printf("The address of the array x: %p\n", x);

    // int y[4] = {1,2,3,4};

    // printf("y = &y[0] -> %d\n", y == &y[0]); // the array name is a pointer that stores the address of the first element of the array
    // printf("*y = y[0] -> %d\n", *y == y[0]); // the value that is stored in the address that the array name points to is the value of the first element.
    // printf("y+1 == &y[1] -> %d\n", y+1 == &y[1]);
    // printf("*(y+1) == y[1] -> %d\n", *(y+1) == y[1]);

    








    return 0;
}